theme: jekyll-theme-slate
title: BIGJRA's Walkthroughs
description: In-depth 100% guides for Pokemon Reborn & Rejuvenation
github:
  is_project_page: true

discord:
  url: https://discord.gg/3r83avH4sv

# Build settings
theme: jekyll-theme-slate

plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - raw
  - utils/
  - resources/
  - TODO.md
  - requirements.txt
  
#------------------------------------------------------------------------------------#
# BIGJRA.github.io generator functionality configuration                             #
#------------------------------------------------------------------------------------#

# Local scripts directory: Needed to generate content. Replace as necessary if this moves.
scripts_dir: "C:/Users/jakes/reborn/Scripts"
